# Project Instructions

## Checks

- Run `./check` before finishing code changes.
- The script checks `go fix`, formatting, `go vet`, `staticcheck`, `gopls`, `golangci-lint`, dead code, builds, and tests.
- It is non-mutating and continues after failures so all problems are reported in one run.
