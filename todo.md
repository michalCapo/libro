--zen
when applications are in zen mode its not clear which app is currently selected because we are hiding apps title. we needd another way how to visually show to user which application is selected
when electron app is zen mode and user creates new app this will be created with app title, make sure in this case applciation will be created without title

--browser
when user open browser component and start typing search query instead of url address he gets an err name not resolves. if url is not proper url address search for given text in google

--add
make sure when user add and app via 'add app' button and its not project specific it will be visible in 'apps list page' not user can see the applicaiton win 'apps search' popup but cannot see in 'apps list' page
