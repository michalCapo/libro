#open
Add 'reopen' button on empty projects screen next to 'add app' and 'quick ...' buttons. this should invoke 'open command'. make it only visible when closed apps were stored and open command will work
