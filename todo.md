--url
using url popup on browser app will not load user defined url. its basically working, but not every time. user write url hits enter but nothig will change. Also reload shortcut not working in vibrowser app.

--popup
when popup is specific to selected app, like resizing, chagne URL, .... this should be displayed only in app iframe not as whole app popup. change this behaviour. when popup is not app related keep displaying as it is

--resize
not working on new apps

--center
try to keep select app in center of the screen, now happens thath selected app is on the left side of the screen. user have 5 applications open

--anim
reduce annimations to minimum duration, but keep it noticable, also add animation when switching apps withing project. app swithcing aplication is to slow make it fast, and also remove the white flicker when selected app, this is annying.
