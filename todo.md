--browser
when user use search feature in browser app and ends on link, hitting enter should follow the link but its not working currently

--close
i have defined win+q in gnome4 to close window also ctrl+shift+q will close electron window. so keep win+q to close electron windows (like previously it was implemented, or keep it on gnome4). remove the ctrl+shift+q to clsoe electron window (not needed). add win+w to close the currently selected app. update readme and shortctu popup
