--apps
currently applications search is done via search popup (win+/ shotrcut). change it to: depending on currently selected app (or none). when user hits win+n shotcut next app will be created on right side of currently active app. when user hits win+ctrl+n new app will be crate to the left of the current app. Update shotctut popup. update search popup and remove 'enter' and 'ctrl+enter' keyboard shortcuts, only enter will be available. other functionality/behaviour should stay unchanged

--shortcut
remove ctrl+1..9 shortcuts to switch application. User this shocrtcuts for switching project. ctrl+1 will be home project everytime. display small number next to project names make it visuualy appealing. update shortcuts popup. ctrl+0 will go to last project where new app was craated, keep track of project where last app was created.
Remove win+j/k shortcuts used for switching projects.

--close
now default close shortcut for my gnome4 is win+q. electron app is preventing me to close whole application/window when some ttyd applications are remaining. this is fine. when user hits the close button ask user if he wants to close all application. yes/no question. Also list all opened applications with proejct tree like view, so user will know which project and which app is running. when user hits yes close this application then close windows.
