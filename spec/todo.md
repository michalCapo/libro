--search
browse search feature is to slow and buggy. when user search for something. then search for second time whole electron app will freeze. user have to close it and start again.
'n' shortcut for search next is working but 'shift+n' to search for previous find is not working at all

--web
user can click on absolute button (bottom right) to open developer console for given window. when developer console is not open show count if errors and warnings next to this button

--tree
add support to list and switch between multiple git worktress withing the same "project/directroy" show worktrees to application (even when user is not at master branch). user can select another worktree and app will create new "virtual project" which will be boudn to existing project when its related by worktree. add shortcut to get list of git worktreen as popup. user can search by typing name and hit enter to go to this workgree drirectory. user can add new worktree and also delete it.
Move appliction list (from both side) to the top header menu. Move project list from top menu header to the left side and make it vertical. List git worktree as tree view for given project.
