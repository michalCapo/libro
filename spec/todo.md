--max
when user is in fullscreen mode and he aciidantelly hits `esc` keyboard fullscreen mode will exist, can we suppores pressing this esc key so user need to click on the maximize button (exit) when he want go back to normal mode?
