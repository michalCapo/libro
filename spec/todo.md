--deploy
make me a deploy script which will pump-up version by 0.0.1 and push it to git, similar how deploy script for github/aximo project is working find it in my code/ folder. start at version 0.0.1

--desktop
create me a proper desktop application from this project. look how github/aximo project is done (in my code/ folder) and use the same approach

--install
create me and install script which will compile binary for my os (or any os) and move it into ~/.local/bin/ folder (make sure this will also work for windows and mac)
