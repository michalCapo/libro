// Package components contains the reusable UI dialogs, popups, and helpers
// rendered by the Libro host page (add app, close confirmation, command
// palette, directory browser, project picker, resize popup, search, shortcuts
// reference, and URL popup), along with shared element IDs and small
// JavaScript utilities used across them.
package components
